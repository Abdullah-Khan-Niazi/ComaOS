<UserControl x:Class="ComaOS.UI.MVVM.Views.Apps.MinesweeperView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="400"
             Background="Transparent"
             Loaded="UserControl_Loaded">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Background="#252535" CornerRadius="8" Padding="15" Margin="0,0,0,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Mines Counter -->
                <Border Background="#1a1a2a" CornerRadius="4" Padding="10,5" HorizontalAlignment="Left">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="💣" FontSize="16" VerticalAlignment="Center" Margin="0,0,5,0"/>
                        <TextBlock x:Name="MineCount" Text="10" FontSize="20" Foreground="#f87171" 
                                   FontFamily="Consolas" FontWeight="Bold" VerticalAlignment="Center"/>
                    </StackPanel>
                </Border>

                <!-- New Game Button -->
                <Button Grid.Column="1" x:Name="FaceButton" Content="😊" FontSize="24" 
                        Click="NewGame_Click" Style="{StaticResource GameButtonStyle}" 
                        Width="45" Height="45"/>

                <!-- Timer -->
                <Border Grid.Column="2" Background="#1a1a2a" CornerRadius="4" Padding="10,5" HorizontalAlignment="Right">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="⏱️" FontSize="16" VerticalAlignment="Center" Margin="0,0,5,0"/>
                        <TextBlock x:Name="TimerText" Text="000" FontSize="20" Foreground="#60a5fa" 
                                   FontFamily="Consolas" FontWeight="Bold" VerticalAlignment="Center"/>
                    </StackPanel>
                </Border>
            </Grid>
        </Border>

        <!-- Game Grid -->
        <Border Grid.Row="1" Background="#252535" CornerRadius="8" Padding="10">
            <Viewbox Stretch="Uniform">
                <UniformGrid x:Name="GameGrid" Rows="9" Columns="9" Width="270" Height="270"/>
            </Viewbox>
        </Border>

        <!-- Footer -->
        <Border Grid.Row="2" Background="#252535" CornerRadius="8" Padding="10" Margin="0,10,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <TextBlock x:Name="StatusText" Text="Click a cell to start!" Foreground="#888" FontSize="12" VerticalAlignment="Center"/>
                
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <TextBlock Text="Difficulty:" Foreground="#666" FontSize="11" VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <ComboBox x:Name="DifficultyCombo" Width="80" SelectionChanged="Difficulty_Changed">
                        <ComboBoxItem Content="Easy" IsSelected="True"/>
                        <ComboBoxItem Content="Medium"/>
                        <ComboBoxItem Content="Hard"/>
                    </ComboBox>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>
