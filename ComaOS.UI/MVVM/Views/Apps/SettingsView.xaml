<UserControl x:Class="ComaOS.UI.MVVM.Views.Apps.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="500"
             Background="Transparent"
             Loaded="UserControl_Loaded">
    
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="5">
            <!-- Header -->
            <TextBlock Text="⚙️ Settings" FontSize="24" Foreground="White" Margin="0,0,0,20"/>

            <!-- Display Section -->
            <Border Background="#252535" CornerRadius="8" Padding="15" Margin="0,0,0,15">
                <StackPanel>
                    <TextBlock Text="🖥️ Display" FontWeight="SemiBold" Foreground="#60a5fa" FontSize="14" Margin="0,0,0,15"/>
                    
                    <Grid Margin="0,0,0,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel>
                            <TextBlock Text="Dark Mode" Foreground="White"/>
                            <TextBlock Text="Use dark theme for all windows" Foreground="#666" FontSize="11"/>
                        </StackPanel>
                        <ToggleButton x:Name="DarkModeToggle" Grid.Column="1" IsChecked="True" Style="{StaticResource ToggleSwitchStyle}"/>
                    </Grid>

                    <Grid Margin="0,0,0,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel>
                            <TextBlock Text="Show Desktop Icons" Foreground="White"/>
                            <TextBlock Text="Display application icons on desktop" Foreground="#666" FontSize="11"/>
                        </StackPanel>
                        <ToggleButton x:Name="DesktopIconsToggle" Grid.Column="1" IsChecked="True" Style="{StaticResource ToggleSwitchStyle}"/>
                    </Grid>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel>
                            <TextBlock Text="Animations" Foreground="White"/>
                            <TextBlock Text="Enable window animations" Foreground="#666" FontSize="11"/>
                        </StackPanel>
                        <ToggleButton x:Name="AnimationsToggle" Grid.Column="1" IsChecked="True" Style="{StaticResource ToggleSwitchStyle}"/>
                    </Grid>
                </StackPanel>
            </Border>

            <!-- System Section -->
            <Border Background="#252535" CornerRadius="8" Padding="15" Margin="0,0,0,15">
                <StackPanel>
                    <TextBlock Text="💻 System" FontWeight="SemiBold" Foreground="#60a5fa" FontSize="14" Margin="0,0,0,15"/>
                    
                    <Grid Margin="0,0,0,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel>
                            <TextBlock Text="Operating Mode" Foreground="White"/>
                            <TextBlock Text="Current kernel operating mode" Foreground="#666" FontSize="11"/>
                        </StackPanel>
                        <TextBlock x:Name="ModeText" Grid.Column="1" Text="User" Foreground="#f472b6" VerticalAlignment="Center"/>
                    </Grid>

                    <Grid Margin="0,0,0,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel>
                            <TextBlock Text="Auto-start System Monitor" Foreground="White"/>
                            <TextBlock Text="Launch System Monitor on boot" Foreground="#666" FontSize="11"/>
                        </StackPanel>
                        <ToggleButton x:Name="AutoStartMonitorToggle" Grid.Column="1" IsChecked="False" Style="{StaticResource ToggleSwitchStyle}"/>
                    </Grid>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel>
                            <TextBlock Text="Developer Mode" Foreground="White"/>
                            <TextBlock Text="Show debug information" Foreground="#666" FontSize="11"/>
                        </StackPanel>
                        <ToggleButton x:Name="DevModeToggle" Grid.Column="1" IsChecked="False" Style="{StaticResource ToggleSwitchStyle}"/>
                    </Grid>
                </StackPanel>
            </Border>

            <!-- Sound Section -->
            <Border Background="#252535" CornerRadius="8" Padding="15" Margin="0,0,0,15">
                <StackPanel>
                    <TextBlock Text="🔊 Sound" FontWeight="SemiBold" Foreground="#60a5fa" FontSize="14" Margin="0,0,0,15"/>
                    
                    <Grid Margin="0,0,0,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel>
                            <TextBlock Text="System Sounds" Foreground="White"/>
                            <TextBlock Text="Play sounds for system events" Foreground="#666" FontSize="11"/>
                        </StackPanel>
                        <ToggleButton x:Name="SystemSoundsToggle" Grid.Column="1" IsChecked="True" Style="{StaticResource ToggleSwitchStyle}"/>
                    </Grid>

                    <StackPanel>
                        <TextBlock Text="Volume" Foreground="White" Margin="0,0,0,5"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="50"/>
                            </Grid.ColumnDefinitions>
                            <Slider x:Name="VolumeSlider" Minimum="0" Maximum="100" Value="75" Style="{StaticResource ModernSliderStyle}"/>
                            <TextBlock x:Name="VolumeText" Grid.Column="1" Text="75%" Foreground="#888" TextAlignment="Right" VerticalAlignment="Center"/>
                        </Grid>
                    </StackPanel>
                </StackPanel>
            </Border>

            <!-- Privacy Section -->
            <Border Background="#252535" CornerRadius="8" Padding="15" Margin="0,0,0,15">
                <StackPanel>
                    <TextBlock Text="🔒 Privacy" FontWeight="SemiBold" Foreground="#60a5fa" FontSize="14" Margin="0,0,0,15"/>
                    
                    <Grid Margin="0,0,0,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel>
                            <TextBlock Text="Save Session" Foreground="White"/>
                            <TextBlock Text="Remember open applications on restart" Foreground="#666" FontSize="11"/>
                        </StackPanel>
                        <ToggleButton x:Name="SaveSessionToggle" Grid.Column="1" IsChecked="True" Style="{StaticResource ToggleSwitchStyle}"/>
                    </Grid>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel>
                            <TextBlock Text="Telemetry" Foreground="White"/>
                            <TextBlock Text="Send anonymous usage data" Foreground="#666" FontSize="11"/>
                        </StackPanel>
                        <ToggleButton x:Name="TelemetryToggle" Grid.Column="1" IsChecked="False" Style="{StaticResource ToggleSwitchStyle}"/>
                    </Grid>
                </StackPanel>
            </Border>

            <!-- About Section -->
            <Border Background="#252535" CornerRadius="8" Padding="15" Margin="0,0,0,15">
                <StackPanel>
                    <TextBlock Text="ℹ️ About" FontWeight="SemiBold" Foreground="#60a5fa" FontSize="14" Margin="0,0,0,15"/>
                    
                    <StackPanel Margin="0,0,0,10">
                        <TextBlock Text="ComaOS" FontSize="18" Foreground="White" FontWeight="SemiBold"/>
                        <TextBlock Text="Version 1.0.0" Foreground="#888"/>
                    </StackPanel>

                    <TextBlock Text="Operating System Simulator" Foreground="#666" Margin="0,0,0,5"/>
                    <TextBlock Text="100% Vibe Coded by Claude Opus 4.5" Foreground="#666" Margin="0,0,0,5"/>
                    <TextBlock Text="&quot;Vulnerability as a Service&quot;" Foreground="#f472b6" FontStyle="Italic" Margin="0,0,0,15"/>

                    <StackPanel>
                        <TextBlock Foreground="#888" FontSize="11">
                            <Run Text="RAM: "/><Run x:Name="RamDisplay" Text="0 MB"/>
                        </TextBlock>
                        <TextBlock Foreground="#888" FontSize="11">
                            <Run Text="HDD: "/><Run x:Name="HddDisplay" Text="0 GB"/>
                        </TextBlock>
                        <TextBlock Foreground="#888" FontSize="11">
                            <Run Text="CPU: "/><Run x:Name="CpuDisplay" Text="0 Cores"/>
                        </TextBlock>
                    </StackPanel>
                </StackPanel>
            </Border>

            <!-- Danger Zone -->
            <Border Background="#2a1f1f" CornerRadius="8" Padding="15" BorderBrush="#5a3030" BorderThickness="1">
                <StackPanel>
                    <TextBlock Text="⚠️ Danger Zone" FontWeight="SemiBold" Foreground="#f87171" FontSize="14" Margin="0,0,0,15"/>
                    
                    <Button Content="🗑️ Clear All Memory" Click="ClearMemory_Click" 
                            Style="{StaticResource DangerButtonStyle}" Margin="0,0,0,10"/>
                    <Button Content="💾 Clear All Files" Click="ClearFiles_Click" 
                            Style="{StaticResource DangerButtonStyle}" Margin="0,0,0,10"/>
                    <Button Content="🔄 Restart System" Click="Restart_Click" 
                            Style="{StaticResource DangerButtonStyle}"/>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>
