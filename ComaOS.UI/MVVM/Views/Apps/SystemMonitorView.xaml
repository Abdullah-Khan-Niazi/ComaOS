<UserControl x:Class="ComaOS.UI.MVVM.Views.Apps.SystemMonitorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="600"
             Background="Transparent"
             Loaded="UserControl_Loaded"
             Unloaded="UserControl_Unloaded">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- System Stats -->
        <Grid Grid.Row="0" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- CPU Panel -->
            <Border Background="#252535" CornerRadius="8" Padding="15" Margin="0,0,7,0">
                <StackPanel>
                    <TextBlock Text="CPU Usage" Foreground="#888" FontSize="12" Margin="0,0,0,8"/>
                    <Grid>
                        <ProgressBar x:Name="CpuBar" Value="0" Height="20" Style="{StaticResource SystemProgressBarStyle}"/>
                        <TextBlock x:Name="CpuText" Text="0%" HorizontalAlignment="Center" VerticalAlignment="Center" 
                                   Foreground="White" FontSize="11" FontWeight="Bold"/>
                    </Grid>
                    <TextBlock x:Name="CpuInfo" Text="4 Cores" Foreground="#666" FontSize="11" Margin="0,5,0,0"/>
                </StackPanel>
            </Border>

            <!-- RAM Panel -->
            <Border Grid.Column="1" Background="#252535" CornerRadius="8" Padding="15" Margin="7,0,0,0">
                <StackPanel>
                    <TextBlock Text="Memory Usage" Foreground="#888" FontSize="12" Margin="0,0,0,8"/>
                    <Grid>
                        <ProgressBar x:Name="RamBar" Value="0" Height="20" Style="{StaticResource MemoryProgressBarStyle}"/>
                        <TextBlock x:Name="RamText" Text="0%" HorizontalAlignment="Center" VerticalAlignment="Center" 
                                   Foreground="White" FontSize="11" FontWeight="Bold"/>
                    </Grid>
                    <TextBlock x:Name="RamInfo" Text="0 MB / 0 MB" Foreground="#666" FontSize="11" Margin="0,5,0,0"/>
                </StackPanel>
            </Border>
        </Grid>

        <!-- Toolbar -->
        <Border Grid.Row="1" Background="#252535" CornerRadius="4" Padding="10" Margin="0,0,0,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="Running Processes" Foreground="White" FontSize="14" VerticalAlignment="Center"/>
                <Button Grid.Column="1" Content="🗑️ End Task" Click="EndTask_Click" Style="{StaticResource ToolbarButtonStyle}"/>
            </Grid>
        </Border>

        <!-- Process List -->
        <Border Grid.Row="2" Background="#1a1a2a" CornerRadius="4">
            <DataGrid x:Name="ProcessGrid"
                      AutoGenerateColumns="False"
                      Background="Transparent"
                      BorderThickness="0"
                      HeadersVisibility="Column"
                      GridLinesVisibility="None"
                      SelectionMode="Single"
                      RowBackground="#1a1a2a"
                      AlternatingRowBackground="#1f1f2f"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      IsReadOnly="True">
                <DataGrid.Resources>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="#252535"/>
                        <Setter Property="Foreground" Value="#888"/>
                        <Setter Property="Padding" Value="10,8"/>
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="FontSize" Value="11"/>
                    </Style>
                    <Style TargetType="DataGridCell">
                        <Setter Property="Foreground" Value="White"/>
                        <Setter Property="Padding" Value="10,5"/>
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="DataGridCell">
                                    <Border Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                                        <ContentPresenter VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="#3a3a5a"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                    <Style TargetType="DataGridRow">
                        <Setter Property="Background" Value="Transparent"/>
                        <Style.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="#3a3a5a"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#2a2a4a"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.Resources>
                <DataGrid.Columns>
                    <DataGridTextColumn Header="PID" Binding="{Binding ProcessId}" Width="60"/>
                    <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*"/>
                    <DataGridTextColumn Header="State" Binding="{Binding StateText}" Width="80"/>
                    <DataGridTextColumn Header="Priority" Binding="{Binding PriorityText}" Width="80"/>
                    <DataGridTextColumn Header="Memory" Binding="{Binding MemoryText}" Width="80"/>
                    <DataGridTextColumn Header="Progress" Binding="{Binding ProgressText}" Width="70"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>
