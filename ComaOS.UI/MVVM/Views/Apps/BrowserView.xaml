<UserControl x:Class="ComaOS.UI.MVVM.Views.Apps.BrowserView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="400" d:DesignWidth="600"
             Background="Transparent">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- URL Bar -->
        <Border Background="#252535" CornerRadius="4" Padding="8" Margin="0,0,0,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <Button Content="⬅️" Click="Back_Click" Style="{StaticResource ToolbarButtonStyle}" Width="30" Margin="0,0,5,0"/>
                <Button Grid.Column="1" Content="➡️" Click="Forward_Click" Style="{StaticResource ToolbarButtonStyle}" Width="30" Margin="0,0,5,0"/>
                <Button Grid.Column="2" Content="🔄" Click="Refresh_Click" Style="{StaticResource ToolbarButtonStyle}" Width="30" Margin="0,0,5,0"/>
                <Button Grid.Column="3" Content="🏠" Click="Home_Click" Style="{StaticResource ToolbarButtonStyle}" Width="30" Margin="0,0,10,0"/>
                
                <Border Grid.Column="4" Background="#1a1a2a" CornerRadius="4" Padding="8,5">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="🔒" FontSize="12" VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <TextBox x:Name="UrlBox" Grid.Column="1" 
                                 Text="https://comaos.local"
                                 Background="Transparent"
                                 Foreground="White"
                                 BorderThickness="0"
                                 FontSize="13"
                                 VerticalAlignment="Center"
                                 KeyDown="UrlBox_KeyDown"/>
                    </Grid>
                </Border>
                
                <Button Grid.Column="5" Content="Go" Click="Go_Click" Style="{StaticResource ToolbarButtonStyle}" Width="40" Margin="10,0,0,0"/>
            </Grid>
        </Border>

        <!-- Browser Content -->
        <Border Grid.Row="1" Background="#252535" CornerRadius="4">
            <Grid x:Name="ContentArea">
                <!-- Default Home Page -->
                <StackPanel x:Name="HomePage" VerticalAlignment="Center" HorizontalAlignment="Center">
                    <TextBlock Text="🌐" FontSize="64" HorizontalAlignment="Center"/>
                    <TextBlock Text="ComaOS Browser" FontSize="28" Foreground="White" HorizontalAlignment="Center" Margin="0,15,0,5"/>
                    <TextBlock Text="The web, simulated." FontSize="14" Foreground="#666" HorizontalAlignment="Center" Margin="0,0,0,30"/>
                    
                    <!-- Quick Links -->
                    <TextBlock Text="Quick Links" Foreground="#888" FontSize="12" Margin="0,0,0,10"/>
                    <WrapPanel HorizontalAlignment="Center">
                        <Button Content="📧 Mail" Click="QuickLink_Click" Tag="mail.comaos.local" Style="{StaticResource QuickLinkButtonStyle}" Margin="5"/>
                        <Button Content="📰 News" Click="QuickLink_Click" Tag="news.comaos.local" Style="{StaticResource QuickLinkButtonStyle}" Margin="5"/>
                        <Button Content="🎮 Games" Click="QuickLink_Click" Tag="games.comaos.local" Style="{StaticResource QuickLinkButtonStyle}" Margin="5"/>
                        <Button Content="📺 Video" Click="QuickLink_Click" Tag="video.comaos.local" Style="{StaticResource QuickLinkButtonStyle}" Margin="5"/>
                        <Button Content="🛒 Shop" Click="QuickLink_Click" Tag="shop.comaos.local" Style="{StaticResource QuickLinkButtonStyle}" Margin="5"/>
                    </WrapPanel>
                </StackPanel>

                <!-- Web Page Content (shown when navigating) -->
                <Border x:Name="WebPageContent" Visibility="Collapsed" Background="#1a1a2a" CornerRadius="4" Margin="10">
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <StackPanel x:Name="PageContent" Margin="20">
                            <!-- Dynamic content goes here -->
                        </StackPanel>
                    </ScrollViewer>
                </Border>

                <!-- Loading indicator -->
                <Border x:Name="LoadingIndicator" Visibility="Collapsed" Background="#1e1e2e80">
                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                        <TextBlock Text="🔄" FontSize="32" HorizontalAlignment="Center">
                            <TextBlock.RenderTransform>
                                <RotateTransform x:Name="LoadingRotation"/>
                            </TextBlock.RenderTransform>
                        </TextBlock>
                        <TextBlock Text="Loading..." Foreground="White" Margin="0,10,0,0"/>
                    </StackPanel>
                </Border>
            </Grid>
        </Border>
    </Grid>
</UserControl>
