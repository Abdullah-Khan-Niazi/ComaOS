<UserControl x:Class="ComaOS.UI.MVVM.Views.Apps.FileManagerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="400" d:DesignWidth="600"
             Background="Transparent"
             Loaded="UserControl_Loaded">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Toolbar -->
        <Border Grid.Row="0" Background="#252535" Padding="5">
            <StackPanel Orientation="Horizontal">
                <Button Content="⬅️" Click="Back_Click" Style="{StaticResource ToolbarButtonStyle}" Width="35" Margin="0,0,5,0" ToolTip="Back"/>
                <Button Content="⬆️" Click="Up_Click" Style="{StaticResource ToolbarButtonStyle}" Width="35" Margin="0,0,5,0" ToolTip="Up"/>
                <Button Content="🔄" Click="Refresh_Click" Style="{StaticResource ToolbarButtonStyle}" Width="35" Margin="0,0,15,0" ToolTip="Refresh"/>
                <Button Content="📁 New Folder" Click="NewFolder_Click" Style="{StaticResource ToolbarButtonStyle}" Margin="0,0,5,0"/>
                <Button Content="📄 New File" Click="NewFile_Click" Style="{StaticResource ToolbarButtonStyle}" Margin="0,0,5,0"/>
                <Button Content="🗑️ Delete" Click="Delete_Click" Style="{StaticResource ToolbarButtonStyle}"/>
            </StackPanel>
        </Border>

        <!-- Path Bar -->
        <Border Grid.Row="1" Background="#1a1a2a" Padding="10,8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="📍" FontSize="14" VerticalAlignment="Center" Margin="0,0,8,0"/>
                <TextBlock x:Name="PathDisplay" Grid.Column="1" Text="/" Foreground="White" FontSize="13" VerticalAlignment="Center"/>
            </Grid>
        </Border>

        <!-- File List -->
        <Border Grid.Row="2" Background="#1a1a2a" Margin="0,5">
            <ListBox x:Name="FileList"
                     Background="Transparent"
                     BorderThickness="0"
                     SelectionMode="Single"
                     MouseDoubleClick="FileList_DoubleClick">
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Padding" Value="10,8"/>
                        <Setter Property="Foreground" Value="White"/>
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ListBoxItem">
                                    <Border Background="{TemplateBinding Background}" 
                                            Padding="{TemplateBinding Padding}"
                                            BorderThickness="0,0,0,1"
                                            BorderBrush="#2a2a3a">
                                        <ContentPresenter/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="#3a3a5a"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#2a2a4a"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </ListBox.ItemContainerStyle>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="30"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="80"/>
                                <ColumnDefinition Width="120"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="{Binding Icon}" FontSize="16" VerticalAlignment="Center"/>
                            <TextBlock Grid.Column="1" Text="{Binding Name}" FontSize="13" VerticalAlignment="Center"/>
                            <TextBlock Grid.Column="2" Text="{Binding Size}" Foreground="#666" FontSize="11" VerticalAlignment="Center" TextAlignment="Right"/>
                            <TextBlock Grid.Column="3" Text="{Binding Modified}" Foreground="#666" FontSize="11" VerticalAlignment="Center" TextAlignment="Right"/>
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Border>

        <!-- Status Bar -->
        <Border Grid.Row="3" Background="#252535" Padding="10,5">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock x:Name="StatusText" Text="Ready" Foreground="#888" FontSize="11"/>
                <TextBlock x:Name="ItemCount" Grid.Column="1" Text="0 items" Foreground="#666" FontSize="11"/>
            </Grid>
        </Border>
    </Grid>
</UserControl>
