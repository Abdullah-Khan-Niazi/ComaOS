<UserControl x:Class="ComaOS.UI.MVVM.Views.Apps.NotepadView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="400" d:DesignWidth="500"
             Background="Transparent">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Menu Bar -->
        <Border Grid.Row="0" Background="#252535" Padding="5">
            <StackPanel Orientation="Horizontal">
                <Button Content="📄 New" Click="New_Click" Style="{StaticResource ToolbarButtonStyle}" Margin="0,0,5,0"/>
                <Button Content="📂 Open" Click="Open_Click" Style="{StaticResource ToolbarButtonStyle}" Margin="0,0,5,0"/>
                <Button Content="💾 Save" Click="Save_Click" Style="{StaticResource ToolbarButtonStyle}" Margin="0,0,15,0"/>
                <Button Content="↩️ Undo" Click="Undo_Click" Style="{StaticResource ToolbarButtonStyle}" Margin="0,0,5,0"/>
                <Button Content="↪️ Redo" Click="Redo_Click" Style="{StaticResource ToolbarButtonStyle}" Margin="0,0,15,0"/>
                <Button Content="✂️ Cut" Click="Cut_Click" Style="{StaticResource ToolbarButtonStyle}" Margin="0,0,5,0"/>
                <Button Content="📋 Copy" Click="Copy_Click" Style="{StaticResource ToolbarButtonStyle}" Margin="0,0,5,0"/>
                <Button Content="📌 Paste" Click="Paste_Click" Style="{StaticResource ToolbarButtonStyle}"/>
            </StackPanel>
        </Border>

        <!-- File Name Display -->
        <Border Grid.Row="1" Background="#1a1a2a" Padding="10,5">
            <TextBlock x:Name="FileNameDisplay" Text="📝 Untitled.txt" Foreground="#888" FontSize="12"/>
        </Border>

        <!-- Text Editor -->
        <Border Grid.Row="2" Background="#1a1a2a" CornerRadius="4" Margin="0,5,0,5">
            <TextBox x:Name="Editor"
                     AcceptsReturn="True"
                     AcceptsTab="True"
                     TextWrapping="Wrap"
                     VerticalScrollBarVisibility="Auto"
                     HorizontalScrollBarVisibility="Auto"
                     Background="Transparent"
                     Foreground="White"
                     CaretBrush="#60a5fa"
                     FontFamily="Consolas"
                     FontSize="14"
                     Padding="10"
                     BorderThickness="0"
                     UndoLimit="100"
                     TextChanged="Editor_TextChanged"/>
        </Border>

        <!-- Status Bar -->
        <Border Grid.Row="3" Background="#252535" Padding="10,5">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <TextBlock x:Name="ModifiedIndicator" Text="" Foreground="#f472b6" FontSize="11"/>
                <TextBlock x:Name="CharCount" Grid.Column="1" Text="Characters: 0" Foreground="#666" FontSize="11" Margin="0,0,15,0"/>
                <TextBlock x:Name="LineCount" Grid.Column="2" Text="Lines: 1" Foreground="#666" FontSize="11"/>
            </Grid>
        </Border>
    </Grid>
</UserControl>
